package collections;

public class Intro {

    //process is programs that is run by computer. programs have instances. these instances called as process
    //thread is subset of a process. for ex; every tab of chrome browser is a thread of opened chrome process
    //if we use 2 or more thread within 1 process its called MULTI-THREADING
    //LinkedList is doubly linked list. it is used for FASTER removing and inserting data.
    //access&retrieving(get()) to data is FASTER in ArrayList
    //Synchronized: threads are being executed one thread at time in multi-threading environment
    //
    //
    //


}
